/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
} from '@solana/kit';
import { POD_COM_PROGRAM_ADDRESS } from '../programs';
import { expectAddress, getAccountMetaFactory } from '../shared';
export const WITHDRAW_ESCROW_DISCRIMINATOR = new Uint8Array([
  81, 84, 226, 128, 245, 47, 96, 104,
]);
export function getWithdrawEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_ESCROW_DISCRIMINATOR
  );
}
export function getWithdrawEscrowInstructionDataEncoder() {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WITHDRAW_ESCROW_DISCRIMINATOR })
  );
}
export function getWithdrawEscrowInstructionDataDecoder() {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}
export function getWithdrawEscrowInstructionDataCodec() {
  return combineCodec(
    getWithdrawEscrowInstructionDataEncoder(),
    getWithdrawEscrowInstructionDataDecoder()
  );
}
export async function getWithdrawEscrowInstructionAsync(input, config) {
  // Program address.
  const programAddress = config?.programAddress ?? POD_COM_PROGRAM_ADDRESS;
  // Original accounts.
  const originalAccounts = {
    escrowAccount: { value: input.escrowAccount ?? null, isWritable: true },
    channelAccount: { value: input.channelAccount ?? null, isWritable: true },
    depositor: { value: input.depositor ?? null, isWritable: true },
  };
  const accounts = originalAccounts;
  // Original args.
  const args = { ...input };
  // Resolve default values.
  if (!accounts.escrowAccount.value) {
    accounts.escrowAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([101, 115, 99, 114, 111, 119])),
        getAddressEncoder().encode(
          expectAddress(accounts.channelAccount.value)
        ),
        getAddressEncoder().encode(expectAddress(accounts.depositor.value)),
      ],
    });
  }
  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.escrowAccount),
      getAccountMeta(accounts.channelAccount),
      getAccountMeta(accounts.depositor),
    ],
    programAddress,
    data: getWithdrawEscrowInstructionDataEncoder().encode(args),
  };
  return instruction;
}
export function getWithdrawEscrowInstruction(input, config) {
  // Program address.
  const programAddress = config?.programAddress ?? POD_COM_PROGRAM_ADDRESS;
  // Original accounts.
  const originalAccounts = {
    escrowAccount: { value: input.escrowAccount ?? null, isWritable: true },
    channelAccount: { value: input.channelAccount ?? null, isWritable: true },
    depositor: { value: input.depositor ?? null, isWritable: true },
  };
  const accounts = originalAccounts;
  // Original args.
  const args = { ...input };
  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.escrowAccount),
      getAccountMeta(accounts.channelAccount),
      getAccountMeta(accounts.depositor),
    ],
    programAddress,
    data: getWithdrawEscrowInstructionDataEncoder().encode(args),
  };
  return instruction;
}
export function parseWithdrawEscrowInstruction(instruction) {
  if (instruction.accounts.length < 3) {
    throw new Error(
      'WithdrawEscrow instruction: Insufficient accounts provided. Expected at least required accounts for escrow withdrawal'
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts[accountIndex];
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      escrowAccount: getNextAccount(),
      channelAccount: getNextAccount(),
      depositor: getNextAccount(),
    },
    data: getWithdrawEscrowInstructionDataDecoder().decode(instruction.data),
  };
}
//# sourceMappingURL=withdrawEscrow.js.map
