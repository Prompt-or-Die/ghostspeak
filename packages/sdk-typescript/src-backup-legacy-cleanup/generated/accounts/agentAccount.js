/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
} from '@solana/kit';
export const AGENT_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  241, 119, 69, 140, 233, 9, 112, 50,
]);
export function getAgentAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AGENT_ACCOUNT_DISCRIMINATOR
  );
}
export function getAgentAccountEncoder() {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['pubkey', getAddressEncoder()],
      ['capabilities', getU64Encoder()],
      ['reputation', getU64Encoder()],
      ['lastUpdated', getI64Encoder()],
      ['metadataUri', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['invitesSent', getU16Encoder()],
      ['lastInviteAt', getI64Encoder()],
      ['bump', getU8Encoder()],
      ['reserved', getArrayEncoder(getU8Encoder(), { size: 7 })],
    ]),
    (value) => ({ ...value, discriminator: AGENT_ACCOUNT_DISCRIMINATOR })
  );
}
export function getAgentAccountDecoder() {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['pubkey', getAddressDecoder()],
    ['capabilities', getU64Decoder()],
    ['reputation', getU64Decoder()],
    ['lastUpdated', getI64Decoder()],
    ['metadataUri', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['invitesSent', getU16Decoder()],
    ['lastInviteAt', getI64Decoder()],
    ['bump', getU8Decoder()],
    ['reserved', getArrayDecoder(getU8Decoder(), { size: 7 })],
  ]);
}
export function getAgentAccountCodec() {
  return combineCodec(getAgentAccountEncoder(), getAgentAccountDecoder());
}
export function decodeAgentAccount(encodedAccount) {
  return decodeAccount(encodedAccount, getAgentAccountDecoder());
}
export async function fetchAgentAccount(rpc, address, config) {
  const maybeAccount = await fetchMaybeAgentAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}
export async function fetchMaybeAgentAccount(rpc, address, config) {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAgentAccount(maybeAccount);
}
export async function fetchAllAgentAccount(rpc, addresses, config) {
  const maybeAccounts = await fetchAllMaybeAgentAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}
export async function fetchAllMaybeAgentAccount(rpc, addresses, config) {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAgentAccount(maybeAccount));
}
//# sourceMappingURL=agentAccount.js.map
